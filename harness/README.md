# Harness

The harness validates the toolkit without providing an agent execution runtime.
It checks canonical asset metadata, framework manifests, and adapter links.

Run all checks with `npm test`. Add deterministic fixtures under `fixtures/`
when extending contract validation or evaluation scenarios.
