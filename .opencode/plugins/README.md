# OpenCode Plugins

Place project-owned OpenCode plugin modules in this directory. OpenCode
automatically discovers `.ts` and `.js` files here.

Plugins should be introduced only when agents, skills, commands, and policies
cannot express the required behavior. Keep each plugin focused and document
its permissions, external dependencies, and test coverage.
